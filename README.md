# Scrape Attendance Data

This is a Python app that can help you scrape attendance data from baseball-referance.com.

The script is purely written in Python, useing `requests`, so please make sure you've installed it before you run it.

After running the script, a `data/` folder will be created, and csv files will be placed inside.
